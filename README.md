# Qwik Weather App - Steps

1. `npm create qwik@latest`
1. `npm run qwik add tailwind`
1. Add the following to global.css
    ```
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```